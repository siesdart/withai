{
  "name": "withai",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "docker compose up -d --wait redis && turbo run dev",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "test": "turbo run test"
  },
  "devDependencies": {
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "oxlint-tsgolint": "^7.0.2001",
    "turbo": "^2.10.12"
  },
  "packageManager": "pnpm@12.4.1"
}
